{% extends 'base.html.twig' %}

{% block body %}
    <h1>Technology</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technology.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ technology.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('technologie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('technologie_edit', { 'id': technology.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
