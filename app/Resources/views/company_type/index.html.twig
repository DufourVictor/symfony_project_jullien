{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="center-align">
            <h1>{{ 'app.company_type.index.list_company_type'|trans }}</h1>
            <a class="btn-floating btn-large waves-effect waves-light indigo darken-4 modal-trigger"
               href="#modal-add-type">
                <i class="material-icons">add</i>
            </a>
        </div>
        <table class="responsive-table highlight striped">
            <thead>
            <tr>
                <th>{{ 'app.certificate.index.name'|trans }}</th>
                <th>{{ 'commons.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for type in types %}
                <tr>
                    <td>{{ type.name }}</td>
                    <td>
                        <a href="{{ path('company_type_delete', { 'id': type.id }) }}">{{ 'commons.delete'|trans }}</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div id="modal-add-type" class="modal modal-fixed">
        {{ form_start(form) }}
        <div class="modal-content">
            <div class="center-align">
                {{ 'app.company_type.index.add_company_type'|trans }}
            </div>
            {{ form_widget(form) }}
            <input type="submit" value="{{ 'commons.add'|trans }}" class="waves-effect waves-light btn indigo darken-4"/>
            {{ form_end(form) }}
            <a href="#" class="modal-action modal-close waves-effect waves-green btn red darken-4">
                {{ 'commons.back'|trans }}
            </a>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/modal.js') }}"></script>
{% endblock %}
