{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un type d'entreprise</h1>
    <div class="row">
        {{ form_start(form) }}
        <div class="row">
            <div class="col s6">
                {{ form_row(form.name) }}
            </div>
        </div>
        <input type="submit" class="waves-effect waves-light btn indigo darken-4" value="Valider"/>
    </div>
    {{ form_end(form) }}
{% endblock %}
